# Test results

Store one sanitized Markdown record per modern test attempt, named by the test
run identity defined in [`../README.md`](../README.md). Preserve every stopped,
failed, incomplete, and inconclusive attempt. Legacy migrations belong in
[`legacy/`](legacy/README.md).
